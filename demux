ways to write demux
module demux(A,select,out);
  input select[0:1];
  input A;
  output out[0:3];
  
  assign out[0]=A & (select==00);
  assign out[1]=A & (select==01);
  assign out[2]=A & (select==10);
  assign out[3]=A & (select==11);
  end 
endmodule
  
